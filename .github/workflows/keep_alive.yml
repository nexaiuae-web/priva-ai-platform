name: Keep Render Server Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping to Render
        run: |
          curl -s -f https://priva-ai-api.onrender.com/api/health || echo "Ping executed"
